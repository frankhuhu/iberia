<%= form_for(@vendor) do |f| %>
  <% if @vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vendor.errors.count, "error") %> prohibited this vendor from being saved:</h2>

      <ul>
      <% @vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>Vendor Information</p>

  <div class="row">
    <div class="col-sm-4">
      <div class="form-group form-group-default required">
        <label>Vendor ID</label>
        <%= f.text_field :vendor_id, class: 'form-control', :required => true, :readonly=>(@type=='show') %>
      </div>
    </div>
    <div class="col-sm-4">
      <div class="form-group form-group-default">
        <label>Contact</label>
        <%= f.text_field :contact, class: 'form-control', :readonly=>(@type=='show') %>
      </div>
    </div>
    <div class="col-sm-4">
      <div class="form-group form-group-default">
        <label>Company</label>
        <%= f.text_field :company, class: 'form-control', :readonly=>(@type=='show') %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-sm-4">
      <div class="form-group form-group-default">
        <label>Phone</label>
        <%= f.text_field :phone, class: 'form-control', :readonly=>(@type=='show') %>
      </div>
    </div>
    <div class="col-sm-4">
      <div class="form-group form-group-default">
        <label>Fax</label>
        <%= f.text_field :fax, class: 'form-control', :readonly=>(@type=='show') %>
      </div>
    </div>
    <div class="col-sm-4">
      <div class="form-group form-group-default">
        <label>Email</label>
        <%= f.text_field :email, class: 'form-control', :readonly=>(@type=='show') %>
      </div>
    </div>
  </div>

  <div class="form-group form-group-default">
    <label>Address</label>
    <%= f.text_field :address, class: 'form-control', :readonly=>(@type=='show') %>
  </div>

  <br>
  <br>
  <% if @type == 'show' %>
    <%= link_to 'Edit', edit_vendor_path, class: "btn btn-success" %>
  <% else %>
    <%= f.submit class: "btn btn-success", value: "Submit" %>
  <% end %>
  <%= link_to 'Back', vendors_path, class: "btn btn-default" %><br>
<% end %>
